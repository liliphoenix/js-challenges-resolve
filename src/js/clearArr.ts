export const clearArr = () => {
  let sheldon = ['c', 'o', 'o', 'p', 'e', 'r']
  //   sheldon = []
  //   sheldon.splice(0, sheldon.length)
  //   sheldon.length = 0
  //   while(sheldon.length > 0) sheldon.pop()
  //   while(sheldon.length > 0) sheldon.shift()

  console.log(sheldon)
}
